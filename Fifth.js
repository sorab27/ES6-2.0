import {
  radius,
  height,
  length,
  width,
  areaCircle,
  areaCylinder,
  areaRect,
} from "./Fifth1.js";

console.log("Area of circle: " + areaCircle(radius));
console.log("Area of rectangle: " + areaRect(length, width));
console.log("Area of cylinder: " + areaCylinder(radius, height));
